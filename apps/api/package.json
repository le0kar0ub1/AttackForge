{
  "name": "api",
  "version": "1.0.0",
  "description": "AttackForge FastAPI Backend",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "install": "pip install -r requirements.txt",
    "type-check": "echo 'Type checking not configured for Python'",
    "lint": "echo 'Linting not configured for Python'",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true"
  },
  "private": true
} 