{
  "name": "attackforge",
  "version": "1.0.0",
  "private": true,
  "description": "LLM Adversarial Testing Platform - Interactive adversarial testing of LLMs with human-in-the-loop editing capabilities",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "export": "turbo run export",
    "type-check": "turbo run type-check",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "packageManager": "yarn@1.22.19"
} 